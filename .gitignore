node_modules
.DS_Store
*.bak

# Índice local do CodeGraph (regenerado por repo; não versionar)
.codegraph/
dashboard/node_modules
dashboard/dist/
dashboard/.env

# Variáveis globais (chaves de IA, ClickUp da EG)
.env

# Sessões de browser persistentes (cookies/login do Playwright)
_opensquad/_browser_profile/

# Estado de runtime dos squads (lido pelo dashboard, regenerado a cada run)
squads/*/state.json
_opensquad/logs/

# Configs reais de cliente (tokens Kommo/Ads, dados sensíveis).
# Mantém versionado apenas o molde _template/ e o README da carteira.
_opensquad/_memory/clients/*/
!_opensquad/_memory/clients/_template/

# Histórico dos copilots
.claude-code-history
.codex-history
.claude
.codex  
GEMINI.md
AGENTS.md
CLAUDE.md
.gemini

# Graphify local graph artifacts (regenerated per repo)
graphify-out/
.graphify/

# Local agent config generated by some tooling
.agent/

# Local git worktrees for parallel AI agents
..evergreen-bioma-*/

# Dado financeiro PESSOAL do Eduardo — nunca versionar (vai pro Fóton, não pra EG)
Planilha-Orcamentaria.xlsx
*Orcamentaria*.xlsx

# PDFs grandes de referência (docs-fonte) — pesados p/ o histórico do git
_opensquad/_memory/knowledge/inputs-mega-plataforma/*.pdf

.playwright-mcp
bioma-admin.png

# Bioma MVP v0 local/runtime artifacts
bioma/**/.env
bioma/**/.env.*
!bioma/infra/env/*.example.env
bioma/**/.venv/
bioma/**/node_modules/
bioma/**/dist/
bioma/**/tsconfig.tsbuildinfo
bioma/**/.pytest_cache/
bioma/**/__pycache__/

Descritivo-Foton.md

.vercel
.env*.local

Evergreen - Icone