{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m bioma_worker.cli --drain",
    "cronSchedule": "*/5 * * * *",
    "restartPolicyType": "NEVER"
  }
}
